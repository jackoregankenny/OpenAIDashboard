import type { WidgetConfig } from '@/lib/registry/types';

export const widgetConfig: WidgetConfig = {
  id: 'events-calendar',
  name: 'Events Calendar',
  description: 'List of upcoming events with formatted dates, times, and RSVP links.',
  icon: 'CalendarClock',
  category: 'feature',
  tags: ['events', 'calendar', 'schedule'],
  version: '1.0.0',
};
