import type { WidgetConfig } from '@/lib/registry/types';

export const widgetConfig: WidgetConfig = {
  id: 'navigation-top-bar',
  name: 'Top Bar Navigation',
  description: 'Sticky navigation bar with logo, primary links, and quick actions for school sites.',
  icon: 'Navigation',
  category: 'navigation',
  tags: ['header', 'menu', 'sticky'],
  version: '1.0.0',
};
