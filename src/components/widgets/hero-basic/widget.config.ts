import type { WidgetConfig } from '@/lib/registry/types';

export const widgetConfig: WidgetConfig = {
  id: 'hero-basic',
  name: 'Basic Hero',
  description: 'A simple hero section with title, subtitle, and call-to-action button',
  icon: 'Layers',
  category: 'hero',
  tags: ['header', 'landing', 'cta'],
  version: '1.0.0',
};
